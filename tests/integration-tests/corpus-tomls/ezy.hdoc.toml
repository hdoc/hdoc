[project]
name = "ezy"
git_repo_url = "https://github.com/titapo/ezy/"
git_default_branch = "master"

[paths]
compile_commands = "build/compile_commands.json"
output_dir = "../../hdoc-output/ezy"

[ignore]
paths = [
    "/tests/",
]

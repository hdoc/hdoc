[project]
name = "dlib"
version = "0.1.0"

[paths]
compile_commands = "build/compile_commands.json"
output_dir = "../../hdoc-output/dlib"

[includes]
paths = []
use_system_includes = true

[ignore]
paths = [
  "/dlib/external/",
]
